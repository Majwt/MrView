import type { Connection } from "@/components/connections-table/columns";
import type { GraphNode, GraphEdge } from "../graph/types";

export function buildConnections(nodes: GraphNode[], edges: GraphEdge[]): Connection[] {
  return [];
}
